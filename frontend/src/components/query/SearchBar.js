/**
 * SearchBar Component (Compatibility Layer)
 * 
 * This file exists for backwards compatibility.
 * It imports and re-exports the QueryComponent as SearchBar.
 * 
 * All functionality has been migrated to QueryComponent.js
 */

import QueryComponent from './QueryComponent';

// Export the QueryComponent with the name SearchBar
export default QueryComponent;
